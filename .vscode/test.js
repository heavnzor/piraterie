const names = ['Abricot', 'Peche', 'Citron', 'Pamplemousse', 'orange'];
const userName = prompt ('Username ? ');
username === 'admin' ? console.log ('Hi buddy !') : console.log ('Erreur');

console.log (names);
const lengths = [];
for (const name of names) {
  const i = names.indexOf (name);
  const length = names[i].length; //
  console.log (name);
  lengths.push (length);
}
const date = new Date ();
console.log (date);
console.log (lengths);

function username () {
  abricot = true;
}

